# ============================================
# Backend - Variables de entorno
# ============================================

# ---- Base de datos ----
# LOCAL (desarrollo): PostgreSQL en tu máquina
DATABASE_URL=postgresql://erp_admin:CHANGE_ME@localhost:5432/erp_presu

# PRODUCCIÓN (Google Cloud SQL): descomenta y ajusta
# Opción A - Cloud SQL Auth Proxy (recomendado, más seguro):
#   DATABASE_URL=postgresql://erp_admin:CHANGE_ME@127.0.0.1:5432/erp_presu
#   (el proxy se encarga de la conexión segura a Cloud SQL)
# Opción B - IP privada (desde Cloud Run / GKE en la misma VPC):
#   DATABASE_URL=postgresql://erp_admin:CHANGE_ME@CLOUD_SQL_PRIVATE_IP:5432/erp_presu
# Opción C - Cloud SQL con socket Unix (Cloud Run con conector):
#   DATABASE_URL=postgresql://erp_admin:CHANGE_ME@localhost/erp_presu?host=/cloudsql/PROJECT:REGION:INSTANCE

# ---- Google Cloud ----
# ID del proyecto GCP (para Cloud SQL + Firebase)
GCP_PROJECT_ID=erppresu
# Región de Cloud SQL (ej: europe-southwest1 para Madrid)
GCP_REGION=europe-southwest1
# Nombre de la instancia Cloud SQL
CLOUD_SQL_INSTANCE=erp-presu-db

# ---- Firebase ----
FIREBASE_PROJECT_ID=erppresu

# ---- App ----
PORT=4000
NODE_ENV=development
UPLOAD_DIR=./uploads

# Si usas service account para Firebase Admin / Cloud SQL:
# GOOGLE_APPLICATION_CREDENTIALS=./service-account.json

# ---- OrderFlow Firestore (solo lectura - catálogo de materiales) ----
# Proyecto GCP externo donde está la colección 'inventory'
ORDERFLOW_PROJECT_ID=orderflow-pxtw9
ORDERFLOW_CLIENT_EMAIL=firebase-adminsdk-xxxxx@orderflow-pxtw9.iam.gserviceaccount.com
ORDERFLOW_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...tu clave privada...\n-----END PRIVATE KEY-----\n"
